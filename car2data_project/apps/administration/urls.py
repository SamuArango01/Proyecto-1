from django.urls import path
from . import views

app_name = 'administration'

urlpatterns = [
    # Placeholder for administration-related URLs
    # Add your administration URLs here when you create the views
]
